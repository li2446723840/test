var longestPalindrome = function(str) {
  if (str.length <= 1) {
    return str;
  }
  let left = 0;
  let right = 1;
  let temp;
  while(right < str.length) {
    console.log(11);
    right++;
  }
};
console.log(longestPalindrome("abvvbada"));